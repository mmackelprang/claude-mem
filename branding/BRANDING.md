# claude-mem — brand guide

**Proposed display name:** claude-mem
**Tagline:** *Session memory for Claude Code.*

> **Status note:** Fork of thedotmack/claude-mem carrying local fixes. Branding is optional dressing for your copy; the upstream identity stays authoritative.

## Why this name

A fork keeps its upstream's name — renaming a fork severs it from the project it tracks. The mark is local color for your copy only.

## The mark

Session transcripts (the bars) compressing into a small knowledge graph — the whole pipeline in one glyph.

## Palette

| Color | Hex | Role |
|---|---|---|
| Dusk | `#2D2A3E` | Background |
| Fog | `#8D89A6` | Transcript bars |
| Clay | `#D97757` | Graph, accents |

## Files in this directory

| File | Use |
|---|---|
| `logo.svg` | Full lockup (mark + wordmark + tagline) for README headers and docs |
| `favicon.svg` | Square app mark, scales from 16px to full size |
| `favicon.ico` | Legacy multi-size favicon (16/32/48) |
| `favicon-32.png` / `apple-touch-icon.png` / `icon-512.png` | Rendered rasters |

README header: `<p align="center"><img src="branding/logo.svg" alt="claude-mem" width="520"></p>`
Favicon: `<link rel="icon" href="/branding/favicon.svg" type="image/svg+xml">`

Wordmark face: Montserrat Bold (system-sans fallback). The lockup's text is live SVG text;
convert to outlines in any SVG editor if you need pixel-identical rendering everywhere.

---
*Generated as a proposal — names, colors, and marks are suggestions to accept, tweak, or reject.*
