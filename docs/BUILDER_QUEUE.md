# Builder Queue

> Work items scoped + planned by Planner, ready for Builder to ship one PR per row.
> Builder: branch from `main`, implement per the linked plan, run all gates in the plan's § Verification,
> open a PR, and (per the auto-merge policy) merge on green. Do not re-plan — surface drift instead.
>
> **Last updated:** 2026-07-16 (Planner — #18 queued: Mission Control Phase 1, spec + plan; findings #19–#23 filed to Backlog)
>
> **Numbering:** Backlog/Queue `#` values are stable IDs — never reused, gaps are expected once a row
> ships or is dropped. **Recently shipped rows are deliberately unnumbered:** they are not queue rows, so
> they must not consume a Backlog ID (doing so caused the #5 collision between PRs #10 and #11).

## Legend

- 📋 queued · 🚧 in flight · ✅ shipped · ⛔ blocked

## Queue

| # | Status | Item | Spec + Plan | Depends on | Notes |
|---|--------|------|-------------|------------|-------|
| 1 | 📋 | **Fix observation → Chroma vector sync packaging bug** — ship `plugin/sqlite/SessionStore.js` + inline `parseFileList`; add a static packaging guard; make MODULE_NOT_FOUND-class errors visible. Consolidates PRs #3102/#3108/#3116; closes #3091/#3092/#3107. | [`docs/bug-fixes/2026-07-02-chroma-observation-sync-packaging.md`](bug-fixes/2026-07-02-chroma-observation-sync-packaging.md) | — | Fresh consolidated branch, `Co-authored-by:` bionicbutterfly13 + KaizenPrompt + Garcia6l20. **Hybrid** approach: B (static-inline) for `parseFileList`, A (emit + `files` allowlist) for `SessionStore`. **Exclude** #3116's folder-`CLAUDE.md`/`HybridSearchStrategy` changes → item #2. High severity (headline feature broken on every 13.9.x install). |
| 18 | 📋 | **Mission Control — Phase 1 (read/mine only)** — extend the existing worker viewer (D5) with a mined **Attention floor** (open PRs + `Proposed` specs → reviews; error observations → escalations; doc Open-Questions → questions) plus **Observability** read-views: `ProgressQuery` (per-agent × time rollup), `VelocityQuery` (ungrouped shipped-vs-open), `NextStepsFeed` (deduped `session_summaries.next_steps`, currently read by nothing). Creates the `attention_items` table (schema v41) as the store for *mined* items only. | [spec](superpowers/specs/2026-07-16-mission-control-design.md) · [plan](superpowers/plans/2026-07-16-mission-control-phase-1-plan.md) | — | **Read/mine only.** Explicitly NOT in scope (Phases 2–4, each gets its own spec): `attention_raise` emit tool, agent-facing `attention_items` writes, LLM synthesis, roadmap-row linkage, stale detection, new-row proposals, semantic matching, and **ANY write toward `BUILDER_QUEUE.md`**. Plan bakes in spec §8 testing: a hard-tested **shared** `BUILDER_QUEUE.md` parser that emits a LOUD error on parse failure (never a silent empty result — the 2026-07-15 failure mode) and handles the `~~9~~`/`~~15~~` tombstone + unnumbered-shipped-row conventions; `AttentionMiner` idempotency (two passes = no dupes) + auto-resolution (merged PR → resolved); graceful degradation when `gh` is unavailable (R5). |

## Backlog (not yet planned — needs a Planner pass)

| # | Item | Origin | Notes |
|---|------|--------|-------|
| 2 | Empty folder `CLAUDE.md` + `HybridSearchStrategy.isFolder` / metadata-fallback fix | Split out of PR #3116 (Garcia6l20) | Query folder observations by project-relative path; forward `isFolder` through `HybridSearchStrategy.findByFile`; return full metadata on file/folder lookups regardless of Chroma state; never write empty/skeleton folder `CLAUDE.md`. Distinct bug from item #1 — needs its own spec, review, and folder-context UAT. |
| 3 | Workstream 2 — multi-user / cross-team memory (ADR-first) | `HANDOFF_multiuser_and_packaging.md` | Architecture-first; shared store, ID namespacing, author/scoping, auth, migration that does not regress single-user local mode. Investigate existing server mode (`CLAUDE_MEM_SERVER_URL`, `scripts/server-service.cjs`) before designing new. |
| 4 | Workstream 3 — validate cross-session recall empirically | `HANDOFF_multiuser_and_packaging.md` | After item #1 lands and observations flow, confirm prior-session observations are recalled/injected, `observation_search` returns `observation` docs, and project scoping holds across ≥2 projects. |
| 5 | **[P0] R2 summary-lane privacy net is 2/3, not 3/3, on an unconfigured machine** | ADR 0002 §4.3.3 / PR #9 pre-merge review | `markPrivateSession persists privateSession=true on session metadata` (`tests/server/runtime/server-session-runtime.test.ts:239`) is Postgres-gated behind `CLAUDE_MEM_TEST_POSTGRES_URL` (read at :19). Without that env var the suite **silently substitutes a passing placeholder** — `it.skip('requires CLAUDE_MEM_TEST_POSTGRES_URL', () => {})` at :63 — so the run reports green while only 2 of the 3 P0 safety-net tests actually executed. **Mechanism (verified):** the guard is `if (!testDatabaseUrl) { it.skip(...); return; }` at :61–65; that bare `return` exits the `describe` callback, so **all 7 `it()` blocks below it (:105–280) never register at all** — the runner cannot report them as skipped because it never learns they exist. Blast radius is 7 tests, of which `markPrivateSession` is the P0 one. ADR 0002 §4.3.3 assumes all 3 guard every run; they do not. **A silent skip on a P0 privacy guard is indistinguishable from a pass.** Fix either way: (a) CI must supply `CLAUDE_MEM_TEST_POSTGRES_URL` so the lane always runs, and/or (b) the gate must **fail loudly** when unset instead of skipping quietly. Prefer (b) as the invariant — (a) alone still leaves every dev box silently at 2/3. **Highest priority of #5–#10 (Mark's call): a live hole in a P0 guard, not a test-hygiene chore.** |
| 6 | **Decide whether to fix the 4 upstream test defects inherited from v13.11.0** | PR #9 / ADR 0002 §9 — deferred at merge, logged | 3× `tests/services/integrations/spawn-contract-windows.test.ts` (Windows #2695) assert `toBe('cmd.exe')` while the source returns `process.env.ComSpec ?? 'cmd.exe'` (`src/shared/spawn.ts:74`). On real Windows `ComSpec` is `C:\WINDOWS\system32\cmd.exe`, so the assertion **can only pass where `ComSpec` is unset** — i.e. upstream's Linux CI faking `platform:'win32'`. Plus 1× `HealthMonitor > isPortInUse > should honor configured worker host` (`tests/infrastructure/health-monitor.test.ts`, port-probe/env-dependent). Test-only; runtime behaviour (absolute path) is arguably safer than a bare PATH lookup. All four backing files are **byte-identical to upstream `f5633c1f`** (`git diff --name-only f5633c1f HEAD -- <the 4 files>` → empty) — zero fork bytes, which is why the merge was not blocked. **Tradeoff this row must decide, not assume:** fixing upstream-owned tests adds **permanent fork divergence** (ADR §9's accepted cost) and a recurring conflict surface on every future sync. Options: (a) fix in-fork and carry the divergence; (b) upstream the fix and wait; (c) skip-with-reason on Windows; (d) leave red and encode an expected-failure list (see #7). **Decide whether to fix — do not assume yes.** |
| 7 | **Triage the 18 pre-existing test failures on `fork/main` (Windows)** | Measured during PR #9 against a clean `d42aa298` worktree | Predate the v13.11.0 merge — `fork/main` 220 tests / **18 fail**; post-merge branch 225 / 22 (delta +4 = item #6). Clusters: **ChromaMcpManager subprocess-kill (4)**; **HealthMonitor port probing (4)** — root cause identified: `spyOn(net,'createServer')` does not intercept on this box, so the probe hits real sockets; **`runOneTimeV12_4_3Cleanup` (5)**; plus one each of Logger-standards, ProcessManager, Spawn-Contract, hookCommand-stderr, writeJsonFileAtomic. **None touch a privacy path (verified).** Consequence beyond the failures themselves: the auto-merge policy's "full unit suite green" gate is **structurally unmeetable** on this fork, forcing every PR onto a hand-computed "no new regressions" delta — that recurring manual cost is the real damage. Deliverable: a per-cluster fix-or-quarantine call, and if any stay red, an **encoded expected-failure baseline** so the suite can gate automatically again instead of being read by hand. Pairs naturally with #6. |
| 8 | **Postgres test-harness defect — `SET search_path` applied to `client` while the test passes `pool`** | Found during PR #9 §8.2 live-Postgres verification | `processSessionSummaryResponse persists kind=summary observation idempotently` fails with `relation "observation_generation_jobs" does not exist`. Root cause: the test passes `pool` while `SET search_path` was applied only to the `client` — so the pool hands out a **fresh connection still pointed at `public`**, where the table isn't. Harness bug, not product code; **pre-existing, not merge-caused**. Fix options: set `search_path` via the pool's connection options (so it applies to every pooled connection), or have the test use the same `client` the `search_path` was set on. Prefer the pool-options route — the one-shot pattern will silently re-break the moment any other test reaches for `pool`. Worth auditing the Postgres suite for the same client-vs-pool pattern. |
| ~~9~~ | ✅ **Shipped as [PR #11](https://github.com/mmackelprang/claude-mem/pull/11)** — retired, ID not reused. Filed as "`sync-marketplace` is Unix-only"; that framing was **wrong** — rsync was only 1 of 3 independent barriers (`cd ~/…` under cmd.exe, and nothing verifying the sync output is what the hooks load). See **Recently shipped** for the full write-up. | Confirmed live on Mark's Windows box, 2026-07-15 | Kept as a tombstone so #9 is never re-filed or re-used. |
| 10 | **Watch item — Windows spawn path changed under v13.11.0; verify on real Windows during/after the pilot redeploy** | ADR 0002 / PR #9 — Phase 2 watch item | Upstream's shared spawn helper (`src/shared/spawn.ts`) replaced the fork's local wrapper, changing the Windows spawn path from `cmd.exe` to `C:\WINDOWS\system32\cmd.exe` (via `process.env.ComSpec ?? 'cmd.exe'`, :74). **Untested on real Windows** — upstream's CI fakes `platform:'win32'` on Linux, which is precisely the blind spot that makes item #6's tests pass there and fail here. Mark runs Windows, so this is a live surface, not hypothetical. Expected benign (an absolute path is arguably safer than a bare PATH lookup) — but "expected benign" and "verified" are different things, and this is the one behavioural runtime change on the Windows path in the entire merge. **Not a code change — a verification task.** Gate: Phase 2 pilot NAS redeploy (ADR §7.2, needs a Postgres volume snapshot first). Verify spawned subprocesses (Chroma MCP, worker, supervisor) start and terminate cleanly on real Windows. |
| 11 | **[P1] `server-beta` generation worker exits 0 into an infinite restart loop when generation is disabled** | Read-only NAS pilot investigation, 2026-07-15 | `runServerGenerationWorker()` ends at `await new Promise<void>(() => {})` (`src/server/runtime/ServerService.ts:869`) under the comment *"Block forever … Without this the process would exit."* **The comment is factually wrong** — an unsettled Promise is not a libuv handle and holds nothing open. The event loop is held by the BullMQ Worker's blocking Redis connection, which only attaches on the Active path: `create-server-service.ts:212-213` calls `.start()` **only** `if (generationWorkerManager instanceof ActiveServerGenerationWorkerManager)`, and `ActiveServerGenerationWorkerManager.start()` (`:63-77`) is what opens it. Generation disabled → no Worker → no handle → **Node exits 0** → compose `restart: unless-stopped` (`docker-compose.yml`, `claude-mem-worker`) restarts it → forever. **Measured on the pilot:** 445 restarts in 4h; ~2,817/day since 2026-07-04; ~30,000 total; ~200ms per lifetime; ExitCode 0; zero WARN/ERROR in 11 days. **Verified in-repo, and it resolves the obvious objection (an HTTP listener *would* hold the loop open):** this entrypoint is worker-only and, per its own comment at `:830-836`, **"never opens an HTTP listener"** — so the 200-OK `/healthz` comes from the *separate* `claude-mem-server` container. Green health and a crash-looping worker are fully consistent, not contradictory. **Also verified:** `claude-mem-worker` is the **only** long-running service in `docker-compose.yml` with **no `healthcheck:` block** (postgres, valkey, chroma and claude-mem-server all have one) — that omission, not `/healthz`, is why 30k restarts looked healthy. **Caveat — do not build the fix on this:** "the Redis connection is *the* handle" is inference, not measurement; an idle `pg` pool is a competing candidate (no `new Pool` call site found under `src/server/`, so the handle inventory is unconfirmed). The **exit-0 outcome is measured** and is what the fix must address. Fix direction: log `DisabledServerGenerationWorkerManager`'s existing `reason` string (`src/server/runtime/types.ts:64-83` — currently only reachable via `getHealth()`, never logged) at startup, and **refuse to start / exit non-zero** rather than exit 0 silently; correct the false comment at `:867-868`; add the missing worker `healthcheck:`. See also #14 (the empty API key is what disables generation) and #12 (probe blindness). |
| 12 | **[P1] No deploy-facing probe can observe whether generation is actually running** | Read-only NAS pilot investigation, 2026-07-15 | **Filed with a corrected premise — read this before planning.** The originating report framed this as "`/healthz` is structurally blind; consider surfacing generation state via `/v1/info`". **In-repo verification contradicts that fix direction, and it is a trap.** (1) `/v1/info` **already exposes** `boundaries.generationWorkerManager: this.graph.generationWorkerManager.getHealth()` — including the `reason` string — at `src/server/runtime/ServerService.ts:65-80`. The data is not missing. (2) But `docker-compose.yml` sets `CLAUDE_MEM_GENERATION_DISABLED: "true"` on `claude-mem-server` **by design** ("The HTTP service does not consume BullMQ jobs; the worker container does"). So `/v1/info` on the server reports generation `disabled` **always, in a perfectly healthy stack** — wiring that into `/healthz` would produce a permanent false alarm and gate the compose healthcheck on an expected-disabled boundary. (3) The process that actually generates — `claude-mem-worker` — **has no HTTP surface at all** (`ServerService.ts:830-836`, "never opens an HTTP listener") and no compose `healthcheck:`. **That is the real gap: the worker's liveness is structurally unobservable over HTTP, so no endpoint change on the server can fix it.** True statement of the finding: the pilot probed 200/200/200 for 11 days while capturing nothing, and that green health actively misled the investigation — but the cure is a **worker-level** health signal (row #11's healthcheck), not a `/healthz` contract change. This row's deliverable is therefore a **decision**, not a foregone fix: decide whether anything is still owed here **once #11 lands its worker healthcheck** — plausibly nothing, or at most a readiness probe distinguishing "generation disabled by design (server)" from "generation should be running but isn't (worker)". **Do not change `/healthz`'s minimal contract without answering (2).** Sequence after #11. |
| 13 | **[P1] The NAS pilot has never ingested anything — "pilot complete" is unsubstantiated** | Read-only NAS pilot investigation, 2026-07-15 | **Verified read-only on the live box:** `agent_events=0`, `observation_generation_jobs=0`, `server_sessions=0`, `usage_events=0`, `team_members=0`, `teams=1`. Valkey holds only BullMQ `:meta` keys — no `wait`/`active`/`failed`. `audit_log`'s last entry is **2026-07-04**. The only 2 `observations` are `kind=manual` / `created_by_job_id=NULL`, written by since-revoked E2E test keys (`actor-alice`/`actor-bob`) on Jul 3. All 12 API keys are test artifacts. **No client has ever successfully written to that box.** The runbook (on the unmerged branch — see #15) claims *"Pilot is functionally complete: a remote teammate can consume/contribute over Tailscale"*; that was validated by **E2E test writes, not by an actual teammate**. Correct the claim as part of this row. **Prime suspect, but the originating report's description of it is wrong in three ways — corrected here from source (`src/services/hooks/runtime-selector.ts`):** (a) **These are `settings.json` keys, not env vars** — `selectRuntime()` reads `loadFromFileOnce()` (`~/.claude-mem/settings.json`), so checking the shell environment proves nothing. (b) **There are three required keys, not two** — `buildServerContext()` requires `CLAUDE_MEM_SERVER_URL` **and** `CLAUDE_MEM_SERVER_API_KEY` **and** `CLAUDE_MEM_SERVER_PROJECT_ID` (`:60-86`; legacy `*_BETA_*` names accepted as fallback). The report omits `projectId` entirely. (c) **The fallback is NOT uniformly silent** — all three missing-key paths log `logger.warn('HOOK', '[server-fallback] reason=missing_{base_url,api_key,project_id}')` (`:77`, `:80`, `:84`). **Only one path is truly silent:** `resolveRuntimeContext()` returns `{runtime:'worker'}` with **no log whatsoever** when `selectRuntime() !== 'server'` (`:101-103`) — i.e. when `CLAUDE_MEM_RUNTIME` is unset or not `server`/`server-beta`. **This sharpens the investigation into two cheap, ordered steps:** (1) read the client's `~/.claude-mem/settings.json` for `CLAUDE_MEM_RUNTIME` — if absent or not `server`, that is the silent path and **no log will ever exist**, which matches the evidence; (2) if it *is* `server`, grep the **client-side** hook logs for `[server-fallback] reason=` — it names the exact missing key. Note the "zero WARN/ERROR in 11 days" evidence is from the **NAS**; a `[server-fallback]` warn would be on the **client**, so it does not rule (2) out. Scope: find why no client reaches `:37877`, and make **only** the genuinely-silent `resolveRuntimeContext()` branch loud (`:101-103`) — the rest is already loud; do not duplicate logging there. |
| 14 | **[P2] `ANTHROPIC_API_KEY` is empty on the pilot's `claude-mem-worker` → generation off** | Read-only NAS pilot investigation, 2026-07-15 | Declared but empty (value never printed). Confirmed in-repo: `docker-compose.yml` sets `ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:-}` — an **empty-string default**, not unset. Provider is `claude`; `create-server-service.ts:258-259` does `if (!apiKey) return null` → `buildServerGenerationProviderFromEnv()` returns null → `DisabledServerGenerationWorkerManager` ("no server generation provider configured…"). Logged as an open follow-up in the runbook since 2026-07-04, never closed. **Sequencing (important): setting this fixes the crash loop in #11 but fixes ZERO of the reported symptom** — ingest (#13) is dead *upstream* of generation, so an enabled provider would sit idle on an empty queue. **Do #13 first**; this row is worthless until something is actually being ingested. **Latent bug found while verifying this row — fix it here:** `instantiateServerGenerationProvider()` reads `process.env.ANTHROPIC_API_KEY ?? process.env.CLAUDE_MEM_ANTHROPIC_API_KEY ?? ''` (`create-server-service.ts:258`). `??` is **nullish** coalescing, and compose always defines `ANTHROPIC_API_KEY` as at least `""` — an empty string is **not** nullish, so the chain short-circuits and **`CLAUDE_MEM_ANTHROPIC_API_KEY` is unreachable whenever the stack is started via compose**, contradicting compose's own comment ("ANTHROPIC_API_KEY (**or** CLAUDE_MEM_ANTHROPIC_API_KEY)"). Setting only `CLAUDE_MEM_ANTHROPIC_API_KEY` in `.env` would silently do nothing. Logical-OR (`\|\|`) — or an explicit non-empty pick, as `runtime-selector.ts`'s `pickFirstNonEmpty` already does correctly — is the fix. **Same defect on the `gemini` branch** (`:266`) — `GEMINI_API_KEY ?? CLAUDE_MEM_GEMINI_API_KEY`, same compose empty-default. **Host-mutating — needs Mark's explicit OK before touching the NAS.** Pre-existing Postgres rows would also need the one-time Chroma backfill the runbook records as "not built". |
| ~~15~~ | ✅ **Shipped as [PR #14](https://github.com/mmackelprang/claude-mem/pull/14)** — the four runbooks are on `main` at `docs/ops/`, landed **byte-for-byte unmodified** so every existing citation resolves with zero edits. Re-verified **post-merge** against the landed files: `2026-07-03-nas-tailscale-pilot-runbook.md:37` → *"Deploy the server-beta stack … via docker-compose"* (ADR 0002 `:348`); `:49` → the stale-server-bundle FINDING quoted verbatim (ADR `:100`, `:351`, `:363`); `2026-07-06-tailscale-acl-and-rename-runbook.md:41` → *"Complementary to a later switch to Tailscale Serve"* (row #16). **No re-pointing was needed** — landing the files unedited was chosen precisely to keep the line numbers stable. Correcting the runbook's *"functionally complete"* claim was **deliberately left to #13**, which owns it; editing it here would have drifted the cited lines. ~~[P2] Four operational runbooks exist only on the unmerged local branch `docs/ws2-pilot-runbook`~~ | Read-only NAS pilot investigation, 2026-07-15 | **Original finding, for the record:** `docs/ops/{2026-07-03-nas-tailscale-pilot-runbook, 2026-07-04-teammate-onboarding, 2026-07-06-pihole-nas-runbook, 2026-07-06-tailscale-acl-and-rename-runbook}.md` are **not on `main`** — `git ls-tree fork/main docs/` confirms `docs/ops/` does not exist there at all. Meanwhile ADR 0002 (`docs/architecture/decisions/2026-07-14-upstream-v13.11.0-fork-merge.md`, **on `main`**) cites the pilot runbook **6 times, 4 of them with explicit line numbers**: `:7` (Related), `:100` (`runbook:49`, quoted verbatim as the R3 stale-bundle precedent — *"the single most likely way to ship a broken pilot"*), `:348` (`runbook:37`), `:351` (`runbook:49`), `:363` (R3 risk-table row, `runbook:49`), `:406`. So **`main` carries an ADR whose load-bearing citations point at a file nobody on `main` can open** — and `runbook:49` is not a footnote, it is the cited justification for ADR 0002's §4.1 REGEN gate. This directly cost investigation time on 2026-07-15. Scope: land the four runbooks on `main` (merge the branch or cherry-pick the files), then re-point/verify ADR 0002's citations — **line numbers must be re-checked against whatever actually lands**, since they will drift if the files are edited en route. Cheap, unblocks #13's runbook correction and #16's verification. |
| 16 | **[P3] `host_network: true` follow-up — already closed at the ACL layer; confirm application, then close or narrow** | Read-only NAS pilot investigation, 2026-07-15 | The pilot runbook flags: *"host networking exposes ALL NAS host ports to the tailnet — tighten with Tailscale ACLs or switch to Tailscale Serve (`:37877` only) as a follow-up."* **Asked and answered — the later runbook does close it, at the policy layer.** `docs/ops/2026-07-06-tailscale-acl-and-rename-runbook.md` (on branch `docs/ws2-pilot-runbook`, see #15) exists precisely for this: it names the same root cause (*"The claude-mem pilot uses host networking, so when the NAS joined the tailnet its entire host (every port) became reachable"*) and restricts teammates to the single service port via `{ "action": "accept", "src": ["group:cmem-teammates"], "dst": ["claude-mem:37877"] }`, with a verification step (`:37877` works; `:22`/TrueNAS UI/SMB refused for a throwaway test user). **So this row is not "write a mitigation" — it is two narrower questions.** (1) **Was the ACL ever applied?** The runbook is a procedure doc whose steps are all in the Tailscale admin console — **nothing in the repo can prove it was executed**, and #13 establishes that other "complete" pilot claims were not substantiated, so do not assume. Verify in the admin console. (2) **Is the ACL alone sufficient?** It is a single-layer tailnet-policy mitigation; `host_network: true` is still on, so anything that bypasses or misconfigures the ACL re-exposes every host port. The runbook itself calls Tailscale Serve *"complementary"* (`:41`), not redundant — Serve remains the defense-in-depth fix. Decide whether P3 is the right severity **after** answering (1); if the ACL was never applied, this is not P3. |
| 17 | **[P0] Windows: `chroma-mcp` inherits the worker's listening socket — every worker stop orphans :37777 and the next start dies `EADDRINUSE`** | Reproduced live twice on Mark's box, 2026-07-15 (incl. once during a controlled deploy) | **This is not a stale-state bug; it reproduces on every clean restart.** Mechanism: the worker binds `127.0.0.1:37777`, then spawns `chroma-mcp` (via `uvx`), which **inherits the listening socket handle**. When the worker dies, the chroma chain survives and keeps the socket in `LISTENING` with **no acceptor** — so the port is held by a **dead PID**, `curl /api/health` returns nothing (HTTP 000), and every respawn hits `EADDRINUSE` → `exit(1)` → wrapper exits → hooks report `worker unreachable` forever. **Measured:** a worker bound :37777 on 2026-07-07 08:14:08 and spawned chroma-mcp 48s later; the worker died 07-14 22:30; the socket stayed bound by dead PID 13636 for ~15h across **~950 consecutive failed hooks**, blocking three agent teams and capturing zero observations for a full day. Killing the chroma chain (`taskkill /PID <chroma-mcp> /T /F`) frees the port immediately — **this is the proof of attribution**, not inference. **It then reproduced on the very next stop/start.** `d42aa298` does **not** fix it. **Two in-tree facts make this worse than it looks:** (1) `src/services/sync/ChromaMcpManager.ts:141-148` **already documents** that the grandchildren (`uv`, `python`, `chroma-mcp`) re-parent and survive tree-kill — so `taskkill /T`, which walks PPID, **provably cannot reach them**; and `disposeCurrentSubprocess()` only runs at *connect* time, never when the worker dies. (2) `src/services/infrastructure/HealthMonitor.ts:35-48` implements `isPortInUse()` on win32 as an **HTTP health check instead of a bind probe**, so a dead-but-bound port returns `false` — which makes the existing `"Port in use but worker not responding to health checks" → "dead"` diagnosis **unreachable on Windows**, the one platform where this bug bites. On Linux/macOS the real `net.createServer()` probe would have reported it in seconds. **Fix direction (root cause first):** stop the listening socket being inheritable by chroma-mcp/uvx — mark the handle non-inheritable, or bind :37777 *after* spawning children. Then: delete the win32 special case in `HealthMonitor.ts` so all platforms use the real bind probe; on `EADDRINUSE` where the owning PID is dead, detect the orphan and reap surviving `chroma-mcp`/`python` descendants **by image+age, not by PPID tree**; and surface the bind error to `worker-service.cjs start` stdout instead of the fixed `{"message":"Failed to start worker"}` string (the real error goes to `~/.claude-mem/logs/claude-mem-<date>.log`, not `worker-<date>.log` — that split cost most of a day). **Operational note until fixed: every worker restart on Windows needs a manual chroma-chain kill.** Relates to #12 (probe blindness) and #7 (the 4 failing HealthMonitor port-probe tests sit on exactly this code). |
| 19 | **[cost] Server observation-generation model defaults to `claude-sonnet-4-6` — an un-warned 3× cost surprise** | Session cost audit, 2026-07-15/16; cross-refs #14 | **Verified (source + pricing):** `DEFAULT_SERVER_CLAUDE_MODEL = 'claude-sonnet-4-6'` at `src/server/generation/providers/ClaudeObservationProvider.ts:22`, aliased to `DEFAULT_MODEL` (:23), applied as `this.model = options.model ?? DEFAULT_MODEL` (:53). The **only** override is env `CLAUDE_MEM_SERVER_MODEL`, read in all three provider branches of `create-server-service.ts` (claude :261, gemini :268, openrouter :275). It is **NOT** `CLAUDE_MEM_MODEL` (that is the *worker* path, whose default is `claude-haiku-4-5-20251001` per `SettingsDefaultsManager.ts:104`). The server runtime never consults `settings.json` for the model — resolution is env-var → code-default, with no settings tier. Pricing confirmed against the current Anthropic table: Sonnet 4.6 is $3/$15 per-MTok vs Haiku 4.5 $1/$5 = **exactly 3.00×** on both input and output, so the un-overridden default is a flat 3× versus the worker-path Haiku default. **Inferred/reported (not source-derivable):** Mark's measured server-path cost at 645 obs/day of $658/dev-mo (Sonnet) vs $219 (Haiku) — consistent with the verified 3.00× ratio. **Overlaps #14** (empty API key + the `??`-nullish `CLAUDE_MEM_ANTHROPIC_API_KEY` unreachability) — cross-referenced, not duplicated; this row is specifically the *model default* being an un-warned 3×. Fix direction: make the default explicit/warned at worker startup (log the resolved model + that it is the un-overridden default), and/or document prominently in the operator guide / TEAM-CONFIG.md (both already flag it — this row is about the *code* making it loud). |
| 20 | **[cost/correctness] ~47% of generated observations never persist (10,963 emitted vs 5,845 persisted)** — drop cause UNINVESTIGATED | Count audit, 2026-07-01→16 | **Verified (source — candidate drop sites only; the counts are a runtime claim, not source-derivable):** Parse-stage (`src/sdk/parser.ts`): (a) whole-response rejection — `parseAgentXml` returns `{valid:false}` on blank input / missing `<observation>` root / zero parsed blocks (:42-76); `processGeneratedResponse.ts:75-78` turns that into `parse_error` → `markGenerationFailed(retryable:false)` → **the entire batch the model emitted is dropped**; (b) per-observation empty-field skip (:130-136) drops an observation with no title AND no narrative AND empty facts AND empty concepts. Persist-stage (`src/server/generation/processGeneratedResponse.ts`): (c) **`renderObservationContent` (:476-485) builds content from ONLY title/subtitle/narrative/facts — it ignores `concepts`/`files_read`/`files_modified`**, so an observation whose only populated fields are concepts/files renders to an empty string and is dropped by the empty-content guard (:277-279) — the cleanest silent per-observation loss; (d) privacy-scrub drop (:283-286); (e) `generation_key` upsert (:288-292, `observations.ts:112-114`) collapses a *re-processed* job (same jobId+index+content) rather than inserting, so retries don't add rows. **Correction to the framing (Mark, verify me):** the finding cites "DB `content_hash` is fully distinct," which is the **SQLite/worker** path's dedup key; but the strongest concrete drop the sweep found (site c) is in the **server** path (`src/server/**`, which keys on `generation_key`, not `content_hash`). The shared parser drops (a/b) apply to *both* paths; site c applies only if the 10,963-vs-5,845 gap is on the server path. **Confirm which path the counts came from before building a fix** — it changes which site dominates. **Inferred:** the 47% figure and which site dominates require log analysis (parser "Skipping empty observation" counts at `parser.ts:131`; `parse_error` rates) — uninvestigated by design. |
| 21 | **[cost/correctness] Empty summary jobs still bill a real API request** | Session cost audit, 2026-07-15 | **Verified (source):** `skippedAll = context.events.length > 0 && allEventsScrubbedToEmpty` at `src/server/generation/providers/shared/prompt-builder.ts:64`. A summary job with **zero** unprocessed events → `context.events = []` → the event loop (:53-62) never runs → `allEventsScrubbedToEmpty` stays `true` → `skippedAll = (0 > 0) && true = false` → the job is **not** skipped → execution falls through to `postMessages(prompt, signal)` (`ClaudeObservationProvider.ts:75`), a live Anthropic API call whose body is `<!-- empty after privacy stripping -->` (`prompt-builder.ts:81`). No upstream short-circuit: `generateAndPersist` (`ProviderObservationGenerator.ts:260-272`) calls `provider.generate` unconditionally, and `loadEvents` (:557-591) returns `[]` for a zero-event summary job. All three providers share the gate (OpenRouter :72-73, Gemini :148-149). **Precision correction:** skippedAll is false for a zero-event job because the **`.length > 0` conjunct** fails — the guard distinguishes "had events, all privacy-scrubbed → skip" from everything else; a genuinely empty batch falls into "everything else" and bills — *not* because of scrubbing. **Inferred:** whether zero-event summary jobs are actually enqueued in steady state (an enqueue-policy question; `listUnprocessedEvents` excludes completed-job events, so the report's "most summary jobs are billed-and-vacuous" is plausible but unmeasured). Also a correctness smell: a session summary generated from no events. Fix: short-circuit the API call when `events.length === 0`. |
| 22 | **[reliability] Summary lane has no total-size cap → latent unrecoverable context overflow** | Session cost audit, 2026-07-15 | **Verified (source):** `listUnprocessedEvents` caps at `LIMIT 500` (`src/storage/postgres/server-sessions.ts:311`, applied :328); the summary caller passes no `limit` (`ProviderObservationGenerator.ts:569-573`), so the 500 default always applies. The prompt concatenates **every** event (`eventBlocks.join('\n')`, `prompt-builder.ts:81`) with only a **per-event** 16 KiB cap (`MAX_PAYLOAD_CHARS = 16 * 1024`, :41; applied :110-112) — there is **no aggregate/total-size or token cap** anywhere in `buildServerGenerationPrompt` (:43-97). Worst case ≈ 500 × ~16 KiB ≈ ~8 MB of payload text. An overflow classifies as `unrecoverable` (`ClaudeObservationProvider.ts:207-216`; shared `error-classification.ts:109-114` maps 400/413 the same) → **non-retryable** (`ProviderObservationGenerator.ts:234-237` — only `transient`/`rate_limit` retry) → terminal `failed` (`processGeneratedResponse.ts:151-152`). **Precision:** the cap is 16 KiB of **characters** (not tokens), applied to the privacy-**stripped** payload; a 413/generic provider error could trip before the "prompt is too long" message, but both route to the same non-retryable `unrecoverable`, so the conclusion holds. **Inferred:** whether a real backlog actually reaches the context limit depends on live event sizes/counts — the *unbounded concatenation* and *non-retryable classification* are proven; a specific overflow is not. Fix: total-size cap or chunking on the summary lane. |
| 23 | **[security-hygiene] `settings.json` file-mode — finding as originally stated is STALE; re-scoped to Windows-ACL + POSIX race-window** | PR #14 UAT flag, 2026-07-15; re-verified against source 2026-07-16 | **CORRECTION (Mark, you were wrong here — the original claim is FALSE for the current tree):** the installer **does** chmod 0600. `persistServerSettings` writes `CLAUDE_MEM_SERVER_API_KEY` into `~/.claude-mem/settings.json` (`src/services/hooks/server-bootstrap.ts:161,167`) then **immediately `chmodSync(settingsPath, 0o600)` (:171)**; the installer logs "Settings saved with mode 0600." (`install.ts:929-931`). Introduced in commit `34c734a1` (2026-05-29). Provider keys (`ANTHROPIC_API_KEY`) are **not** in settings.json at all — they go to `.env` via `EnvManager`, written `{mode:0o600}` + `chmodSync(0o600)` (`EnvManager.ts:165-166`) with the data dir at 0700 (:140). So "add a chmod" is already done. **Real residual gaps this row should own (verified):** (1) **Windows** — the chmod is a POSIX no-op wrapped in a silent `catch {}` (`server-bootstrap.ts:172-174`); this repo's platform IS Windows, so the 0600 guarantee is effectively **absent** there and protection falls back to user-profile ACLs, not this code. (2) **`mergeSettings` writer** (`install.ts:761-789` → `writeJsonFileAtomic`) applies **no** chmod; a *newly-created* file via that path opens at process umask (~0644). Mitigating: `writeJsonFileAtomic` **preserves an existing file's mode** (`atomic-json.ts:79-86`), so on POSIX the key-holding file still ends at 0600 (either mergeSettings creates 0644 then persistServerSettings tightens to 0600, or persistServerSettings creates 0600 and mergeSettings preserves it). **Exposure windows:** Windows always; plus a brief POSIX interval between a first `mergeSettings` create (0644) and the API-key write+chmod, during which the file exists but does not yet hold the key. **Inferred:** whether PR #14's UAT observed the Windows no-op or a genuine pre-chmod 0644 window. Re-scope: verify + regression-guard + close the Windows-ACL gap (and/or document that Windows relies on profile ACLs); do NOT re-add a chmod that already exists. |

## Recently shipped

| Item | PR | Notes |
|------|----|-------|
| Merge upstream v13.11.0 into fork (ADR 0002) | [#9](https://github.com/mmackelprang/claude-mem/pull/9) — merged `801059db`, 2026-07-15 | Not a queue row; recorded here because Backlog #5–#10 all trace to it. Merged on owner's call: the literal "full suite green" gate is unmeetable (18 pre-existing failures → #7), so the operative gate was **no new fork regressions** — proven empty, all 4 new-failure files byte-identical to upstream `f5633c1f`. R1 + R2 green against live Postgres (**but see #5 — R2's net is 2/3 without `CLAUDE_MEM_TEST_POSTGRES_URL`**). 4 upstream test defects **deferred, not fixed** (→ #6), logged per auto-merge gate 4. Merged with `--merge`, not `--squash`, deliberately: a squash would collapse `4c7f868c` and destroy its parentage of `f5633c1f`, undoing ADR §2's whole purpose. **Phase 2 (pilot NAS redeploy) NOT included** — separate change, gated on a Postgres volume snapshot per §7.2. Nothing pushed to `origin`. |
| **`build-and-sync` never delivered a build on Windows** — three independent barriers, each fatal on its own. | [#11](https://github.com/mmackelprang/claude-mem/pull/11) — merged `399c28e1`, 2026-07-15 | **This shipped Backlog #9**, which is therefore retired (see below) — #9's ID is not reused. Details below. |
| **NAS server setup guide + the four pilot runbooks landed on `main`** | [#14](https://github.com/mmackelprang/claude-mem/pull/14) — 2026-07-15 | **This shipped Backlog #15** (retired above). New: `docs/ops/2026-07-15-nas-server-setup.md` — step-by-step setup for the `claude-mem` app on `nas.lan`, keyed to the failure modes in #11–#17. Docs-only; nothing deployed, nothing touched on the NAS. **Folded #15 in deliberately:** a setup guide that cross-references the runbooks would otherwise have added a *second* doc on `main` with dangling references — reproducing the exact defect #15 exists to fix. **Not placed in root `README.md`**: that file is byte-identical to upstream `f5633c1f` and is the source for 32 generated `docs/i18n/README.*` translations, so fork-only pilot infra there buys permanent divergence + a conflict surface on every upstream sync (ADR 0002 §9's accepted-cost tradeoff, cf. #6) and 32 stale translations. `docs/server.md` is likewise upstream-owned. `docs/ops/` is fork-only with zero upstream footprint. **Documents, does not fix:** the guide states the #11–#17 defects as operational facts with source citations; the fixes remain queued. Its Appendix lists 7 items flagged **unverified** rather than fabricated — nothing in it was executed against the NAS. |

### `build-and-sync` on Windows (shipped #11, retires Backlog #9) — the three barriers (for the record)

Backlog **#9** filed this as "`sync-marketplace` is Unix-only". That framing was **wrong**, and PR #11
shipped the real fix — so #9 is retired here rather than merged as open work. Fixing rsync alone would
**not** have delivered the build; all three barriers had to go:

1. **`rsync` is not installed on Windows.** `scripts/sync-marketplace.cjs` shelled out to it, so
   `build-and-sync` died at step 2. The `&&` chain then skipped the cache sync *and* the worker restart.
2. **`cd ~/…` does not work in npm's Windows shell.** Two separate sites (`bun install` inside the sync
   script, and the `build-and-sync` worker-restart tail) used `cd ~/…`. npm's default script shell on
   Windows is cmd.exe, which does not expand `~` — both failed with "The system cannot find the path
   specified." **independently of the rsync breakage**, so installing rsync would have moved the failure,
   not fixed it.
3. **Nothing verified that the sync's output is what the hooks load.** The hook resolution order
   (`$CLAUDE_PLUGIN_ROOT` → newest-mtime `cache/<version>/` → marketplace) is contractual, and
   `sync-marketplace` did already target `cache/<version>` — but no check compared the built artifact
   against the resolved one, so a sync that never ran, or landed somewhere outranked by a staler cache
   dir, was completely silent. `scripts/verify-plugin-delivery.cjs` now fails the build on any mismatch.

**Not a barrier, contrary to earlier notes:** "sync targets the marketplace dir but the cache dir wins."
`sync-marketplace.cjs` has synced to `cache/<version>` since at least `56db0681`; that step was simply
never reached because barrier 1 aborted the script first.

**Resolution-order decision:** left unchanged (option (a) — sync into the `cache/<version>` dir the hooks
already prefer). The order is pinned as contractual in `src/build/hook-shell-template.ts` and asserted
byte-for-byte by `tests/infrastructure/plugin-distribution.test.ts`. Setting a machine-wide
`CLAUDE_PLUGIN_ROOT` was rejected: it would repoint **every** Claude Code session on the box at the live
working tree, so a mid-edit or half-built state would become the running plugin for every session at once.
